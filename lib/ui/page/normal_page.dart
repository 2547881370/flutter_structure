import 'package:flutter/material.dart';

class NormalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(title: Text("NormalPage"),),
        body:Container(
            child: Text("NormalPage")
        )
    );
  }
}
